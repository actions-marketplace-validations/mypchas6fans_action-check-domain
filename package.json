{
  "name": "action-check-domain",
  "scripts": {
    "build": "ncc build index.js --minify"
  },
  "devDependencies": {
    "@actions/core": "^1.9.1",
    "@vercel/ncc": "^0.34.0",
    "parse-whois": "^1.0.0",
    "whois": "^2.13.9"
  }
}
